AI-901T00-A Introductio to AI in Azure 

https://learn.microsoft.com/en-us/training/courses/ai-901t00?wt.mc_id=esi_ai_content_wwl#course-syllabus


Two Learning Paths
- AI concepts for developers and technology professionals
- Get started with AI applications and agents on Azure



# Intro to generativ AI and agents
# AI concepts for developers and technology professionals



Generative AI and agents

agent:  LLM + instructions + tools
  - engage via prompts


NLP: Natural Language Processing

text >> tokens (words / partial words / punctuation)
text analysis: 
    - language detection
    - test classification (sentiment analysis) 
    - key-term extraction
    - summarization
  statistical and semantic matches from text to LLM


Speech Recognition
- Speech to Text
  - audio waves to text
- Speech Synthesis
  - text tokens to audio signals (speech)


Computer Vision

model trained on labeled images
 - pixels map to labels

types: 
 - image classification
 - object detection 
 - semantic segmentation 
   - identify individual pixels that make up an object 
 - multi-modal models
   - combine visual features and associated text descriptions


Information Extraction:

extract information and assign to fields
 - combine OCR + analytical model 
    - identify text and interprest individual fields (date, etc)


Responsible AI

guardrails to prevent harmful generation 

- fairness
- reliability / safety
- privacy and security
- inclusiveness
- accountability



# Intro to natual language processing concepts


Text Analysis:
 - language detection
 - key term extraction
 - entity detection 
 - PII detection
 - Text classification
 - Sentiment Analysis
 - Text summarization


# Tokenization

words broken down to tokens and given unique id

Text Normalizaton:  remove punctuation, all lower case
Stop Word Removal:  words removed from analysis ("the", "a", "it")
N-gram Extraction:  multi-term phrases "aritificail intelligence"
Stemming         :  consolidate words by stripping endings "s", "ing", "ed"
Lemmatization    :  reduce words to base form ("lemma") uses linguistic rules:
                     "running" ->> "run",  "global" ->> "globe"
Parts of Spech (POS) tagging :  labeling each token with grammatical category (noun, verb, adjective, adverb)

# Statistical text analysis

- count each token (freq analysis)
- bag of words
- Naive Bayes
- sentiment analysis
- TextRank: sentences as nodes and ranked w/ weighted edges

# Semantic language models

meaningful relationships between text tokens
vector based representation of tokens
    - each vector elements "embeds" a semantic attribute
        - paths in multi-dimensional space
        i.e. cat and dog in a similar direction that is different than "tree"
                cosine similarity between cat and dog is higher than tree
    - can do vector arithmatic: 
        i.e. vector for "dog" + vector for "young" == vector for "puppy" 
        i.e. vector for "cat" + vector for "young" == vector for "kitten" 
analogical reasoning:
    puppy is to dog as kitten is to ?
      - kitten - puppy + dog =   >> cat

cosine_similarity(A,B) = (A * B) / ( ||A|| * ||B|| )

A * B == dot product
||A|| == magnitude of vector

Use semantic models for: 
 - text summarization
 - keyword extraction
 - named entity recognition
 - text classification


# Intro to AI speech concepts

Speech-enabled solutions
 - customer service / support
 - voice assited agents
 - meeting transcription
 - healthcare documentation
 - accessability
 - notification / alerts
 - elearning / training

Considerations: 
  - audio quality
  - language detection / support
  - privacy compliance
  - latency expectations
  - accessability standards

## Speech recognition

speech to text 
- capture analog as digital wave form
- convert energy of signal at different frequencies
    -feature values
      -quantify feature strengths to vectors 
    -feed feature vectors into aucoustic model
      -predicts most probably phoeneme for each feature
    -predicted phoenemes fed to language model
    -post processing: capitalization, punctuation etc.

Detailed process: Speech to text
 - capture soundwave via microphone to digital signal
    - sample @ 16k per second (16kHz)
    - store as numeric value
      - filters applied at this point to remove background noise, hums, clicks, etc

 - preprocess waveform 
    - MFCC:  Mel-Frequency Cepstral Coefficients
      - extract technique mimics human ear perception
        - emphasizes frequencies where speech energy concentrates

How MFCC works:
1. divide audio into frames, split into overlapping 20-30 ms windows

2. apply fourier transform: 
  - convert each frame from time domain to frequency domain, which piteches present

3. map to Mel scale
  - adjust frequency bins to human hearing ranges (low pitches better than high ones)

4. extract coefficients: 
  - compute a smallset of numbers (13 coefficients) that summarize the spectral shape of each frame
    ex. 
        frame 100: [ -113.2, 45.3, 12.1, -3.4, .....] 
        frame 101: [ -111.7, 44.3, 11.7, -3.2, .....] 
        frame 102: [ -109.6, 43.7, 11.3, -2.8, .....] 

Acoustic modeling: 
  - models that learn phoenemes from audio features

modern models you transformer architectures
  - process vecotrs and predicts phoeneme

How Transformer Model WOrks: 
  - attention mechanism:
    - model examines surrounding frames to resolve ambiguity 
      /t/ is different in top vs. bat

  - parallel processing: 
    - transformers analyze multiple frame simultaneously

  - contextualized predictions;
    - network learns certain phoeneme sequences occur frequently in natural speech

  - output is probability distribution of phoenems for each audio frame:
      - 80% confidence for /t/ vs 15% for /p/ vs 5% for others

Language Modeling: 
  - applies knowledge of vocabulary, grammar, common word patterns
  - Statistical patterns: 
      - model knows "weather is nice" appears more often than "whether is nice"
  - Context awareness
      - knowing when to expect a verb over a known i.e. "I need to ..."
  - Domain adaptation
      - models trainged on medical terminology improve accuracy

Decoding: 
  - algos that search thru millins of possible word sequences to find best match for acoustic and language predictions
  - Beam search decoding 
    - most common technique
    - short list (beam) of top-scoring partial transcriptions while processing each frame
      - expands hypothesis with next most likely word 
        - prunes low-scoring paths
        - keeps best candidates

Post processing: 
 - decoder produces raw text that requires cleanup
 - applies formating rules and corrections for readability / accuracy
    - capitalization, punctuation, number reformatting, remove inappropriate  words, "three P M to 3 PM", confidence scoring 


speech to text pipeline: 
 capture >> pre-processing >> acoustic modeling >> language modeling >> decoding  >> post-processing 



## Speech synthesis

Process to synthisize speech:

- Standardize the text
  - prepares text for pronunciation by expanding abbreviations, numbers, symbols
      -i.e.  Dr. becomes "Doctor" and "3 items" becomes three items,  "$" to dollars

- Linguistic analysis
  - Map text to phonemes
  - grapheme-to-phoneme (G2P) 
    - conversion maps written letters to pronunciation sounds
    - modern G2P systems use neural networks trained on pronunciation dictionaries

- Prosody generation: determin pronunciation
  - prosody: rhythm, stress, intonation patterns
    - pitch, duration, intensity, pauses, stress patterns

  - NOTE: transformer neural networks used to predict prosody, 
          Transformers excel at understanding context across entire sentences

  - Prosody process
    - input encoding
      - transfromer preceives phoeneme sequence w/ linguistic features
    - contextual analysis 
      - self-attention identifies relationship between words
    - prosody prediction
      - model outputs predicted values for pitch, duration, and energy at each phoeneme
    - style factors
      - considers speaking style (neutral, expressive, etc.)

- Generate audio
  - generates final audio waveform based on phoeneme sequence and prosody specs
  - neural vocoders:  
    - deep learning models that generate audio samples  
      - i.e. WaveNet, WaveGlow, HiFi-GAN
  - synthesis process:
      - acoustic feature generation  
          - acoustic model (transformer) converts phoeneme and prosody targets into 
            mel-spectograms - visual representation of sound frequencies over time
      - vocoding
          - neural vocoder converts mel-spectograms to raw audio waveforms (16kHz - 48kHz samples per second)
      - post processing
          - applies filtering, normalization or audio effect s

speech synthesis pipeline: 
 text normalization >> linguistic analysis  >>  Prosody generation  >> Speech synthesis  





# Introduction to computer vision concepts
https://learn.microsoft.com/en-us/training/modules/introduction-computer-vision/

## Computer vistion tasks / techniques
 - image classification 
    - trained on large number of images w/ text label

 - object detection
    - examine multiple regions in an image to find individual objects 
    - predicts object w/ bounding box around object in image

 - semantic segmentation 
    - model trained to find objects and classify individual pixels of object they belong to in the image
    - precise prediction of the location of objects in the image

 - contextual image analysis 
    - latest multimodal vision models trained to find contextual relationships between objects and text that describes them
      - i.e. apple, man, eating
    - can determine what objects and activies are depicted


## Images and image processing
- images are aray of values 
    - B/W  : one array with 0-255 gray scale (white to black) 
    - color: three arrays RBG with each array having 0-255 scale of R/G/B

- filters alter values of pixels
  - filter can be one ore more arrays of pixel values called kernels
  - kernels are "convolved across image (scanned left to right) 
      - applies weights across image and results in new image 


## Convolutional Neural Networks (CNN)

Train CNN via: 
 1. images w/ labels fed into netowrk 
 2. layers of fileters used to extract features form each images and fed thru network 
    - filter kernesl start w/ randomly assigned weights and generate arrays of numeric values: feature naps
    - additional layers may pool / downsize feature maps to create smaller arrays that emphasize key visual features 
 3. feature maps are flattened into single dimensional array of feature values
 4. feature values are fed into fully connected neural network 
 5.  output layer of the neural network uses a softmax or similar function to produce a result that contains the 
     probablity value for each possible class
 
during training outputs are compared to to actual label.  The loss is calculated and weights of fully connected 
neural network and filter kernels are modified to reduce loss.  Repeat until optimal set of weights has been learned 
and then saved.
 

## Vision transformers and multimodal models

Transformers for Semantic modeling for language
 - represents tokens (word or phrases) as vector-based embedding
 - transformers use "attention" determine context with other tokens
     - embedding value (multi-dimensional vector) applies token to relation of other tokens
     - tokens that are semantically similar (wheel, tire, car) are encoded in similar vector directions 
        vs. unassociated vectors  (car, park, plant)


Semantic model for images:  Vision Transformers
Vision Transformers:
 - trained on large volume of images 
 - transformer extracts patches of pixes (sections of images) and generate linear vector 
    - utilizes locaton within picture
 - contextual relation between vectors
 - vectors embed values like: color, shape, contrast, texture
 - creates a map of visual features: tree, leaf, face, hat, etc.

Multimodal model
 - can combine linguistic and visual vectors using cross-model attention
    - combines language and images in unified spatial vector 
        - vector for word of tree is close to vector for image of tree


## Image Generation
Same multi-modal used to create natual language responses to visual input can be "reversed" 
to generate images from text input. 

Image created via diffusion: 
 - starts with random noise 
 - iteratively removes noise to get to image that is described by text

( NOTE: same process used for creating videos )


# Introduction to AI-powered information extraction concepts
https://learn.microsoft.com/en-us/training/modules/introduction-information-extraction/


overview of information extraction

## OCR: text detection and extraction

Optical Character Recognition pipeline: 
 1. Image acquistion and input
 2. Preprocessing and image enhancement
    - noise reduction
    - contrast adjustment
    - skew correction 
    - resolution optimization 
 3. Text region detection
    - layout analysis 
        - Semantic segmentation networks: U-Newt, Mask R-CNN 
        - Layout analysis: LayoutLM, PubLayNet-trained models
    - text block identification
    - reading order determination
    - region classification
 4. Character recognition and classification
    - feature extraction
    - pattern matching  (ResNet, DenseNet, EfficientNet)
    - context analysis  (LTSM, BERT)
    - confidence scoring  
 5. Output generation and post-prosessing 
    - text compilation (RNNs and Long Short-Term Memory LTSM)
    - format preservation 
    - coordinate mapping 
    - quality validaiton


## Field extraction and mappint
field extraction pipeline 

1. OCR output ingestion
    - raw text context
    - positional metadata
    - confidense score
    - layout information

2. Field detection and candidate indentification
    - template based detection
    - machine learning

3. Field mapping and association
    - key-value pairing techniques
        - proximity analysis
        - linguistic patterns
    - Table and structured content
    - Confidence scoring and validation

4. Data normalization and  mapping and association
    - format standardization
        - data normalizatio
        - currency and numeric processing
        - text standardization
    - data validation and quality assurance
        - rule based validation
        - statistical validation

4. Integration with business processes and systems
    - schema mapping
        - database schemas
        - api payloads
        - message queues
        - field renaming
        - data type conversion 
        - conditional logic 
    - Quality metrics and reporting
        - field confidence scores
        - document-level quality assessment
        - error categorization


## Azure Document Intelligence in Foundry Tools
https://azure.microsoft.com/products/ai-foundry/tools/document-intelligence

## Azure Content Understanding in Foundry Tools
https://azure.microsoft.com/products/ai-foundry/tools/content-understanding




#######################################################################################################
# Getting Started with AI Applicatins and agents on Azure
#######################################################################################################

https://learn.microsoft.com/en-us/training/paths/get-started-ai-apps-agents/

Azure Structure 
                                                                                         ┌|┐        
                                                                                         │|│        
                                                                                         │|│        
                                                                                         │|│        
     ┌─────────────────────────────────────────────────────────────────────────────────┐ │|│        
     │  Tenant                                                                         │ │|│        
     │     ┌────────────────────────────────────────────────────────────────────┐      │ │|│        
     │     │   Subscription                                                     │      │ │|│        
     │     │                                                                    │      │ │|│        
     │     │     ┌──────────────────────────────────────────────────────────┐   │      │ │|│        
     │     │     │   Resource Group                                         │   │      │ │|│        
     │     │     │                                                          │   │      │ │|│        
     │     │     │                                                          │   │      │ │|│        
     │     │     │   ┌──────────┐        ┌──────────┐       ┌──────────┐    │   │      │ │|│        
     │     │     │   │          │        │          │       │   AI     │    │   │      │ │|│        
     │     │     │   │ compute  │        │ storage  │       │          │    │   │      │ │|│        
     │     │     │   │          │        │          │       │ Foundary │    │   │      │ │|│        
     │     │     │   └──────────┘        └──────────┘       └──────────┘    │   │      │ │|│        
     │     │     │                                                          │   │      │ │|│        
     │     │     │                                                          │   │      │ │|│        
     │     │     │                                                          │   │      │ │|│        
     │     │     └──────────────────────────────────────────────────────────┘   │      │ │|│        
     │     │                                                                    │      │ │|│        
     │     │                                                                    │      │ │|│        
     │     └────────────────────────────────────────────────────────────────────┘      │ │|│        
     │                                                                                 │ │|│        
     └─────────────────────────────────────────────────────────────────────────────────┘ │|│        
                                                                                         │|│        
                                                                                         │|│        
                                                                                         └|┘        
                                                                                      Azure Portal  
                                                                                                    
                                                                                                    
## Developing AI Apps on Azure

Security and Networking 
  - Entra ID (RBAC)
  - Azure Key Vault

Hosting and Scaling 
  - Azure Kubernetes Service  (AKS)
  - Azure App Service 

Data Storage
  - Azure SQL Database 
  - CosmosDB
  - Azure Database for Postgres

AI Capabilities
  - Microsoft Foundry 
    - develop and operate AI agents on Azure


## Microsoft Foundry for AI
https://learn.microsoft.com/en-us/training/modules/get-started-with-ai-in-azure/4-microsoft-foundry?pivots=text


Microsoft Foundry is a unified, enterprise-grade platform-as-a-service (PaaS) for 
building, deploying, and managing AI applications and agents. It consolidates models, 
agent orchestration, monitoring, and governance tools in one platform, offering 
production-grade infrastructure and security.

Foundry offers powerful capabilities for developers, including the ability to choose from a 
wide range of models, use those models to build agents, connect those agents to tools, 
and integrate knowledge by using Foundry IQ, the centralized connection point for data sources.


                                                                                             
        Microsoft Foundry                                                                    
      ┌───────────────────────────────────────────────────────┐    
      │                                                       │    
      │   Foundry Resource                                    │    
      │                                                       │    
      │     ┌───────────────────────────────────────┐         │    
      │     │                                       │         │    
      │     │           Foundry Project             │         │    
      │     │                                       │         │    
      │     │    * Models          * Agents         │         │    
      │     │                                       │         │    
      │     │    * Tools           * Knowledge      │         │    
      │     │                      ( Foundry IQ)    │         │    
      │     └───────────────────────────────────────┘         │    
      │                                                       │    
      └───────────────────────────────────────────────────────┘    

Models:
GPT-5, Claude Opus / Sonnet, Mistral, Cohere, Meta LLaMA, Deep Seek, xAI Grok, 
Black Forest Labs, gated Hugging Face Models

Full lifecycle support enables deployment per region, 
customizable deployment types (standard, provisioned, batch), version control, 
governance support w/ Responsible AI and content safety
                                                                                             

Agents:
Agents can: reason over inputs, call tools, interact w/ data and automate workflows 
using built-in orchestration.  Foundry handles underlying corrdination, including: 
 - message threading,
 - tool execution
 - safety controls
 - observability

developers focus on building agent's goals and capabilities. Isomg low-code or code-first workflows, 
teams can create multi-agent systems that work with project resources such as: 
 - documents
 - datasets
 - search indexes
 - connections to external systems
 - integration with Azure Functions / Microsoft Fabric


Tools:
Azure services: speech, vision, language, document intelligence, etc.
> 12 services that can be used separately or together
  - Azure Vision  : analyze images
  - Azure Language: summarize text 
  - Azure Speech  : convert speect to text


Knowledge:
Foundry IQ permission-aware, multi-source knowledge layer that gives agents accuratge, 
grounded answers using an organizations own data. 

Create configurable knowledge base made up of internal / external knowledge sources:
Azure Blob Storage, SharePoint, OneLake, public web data and handles:
 - indexing 
 - doument chunking 
 - vector embedding
 - metadata extraction

When an agent queries knowledge base, Foundry IQ uses agentic retrieval to break the question into 
subqueries, search multiple sources in parallel and return relevant, citation-backed information 
while enforcing user permissions and Microsoft Purview sensitivity labels.

Assets in AI solution are organized within a project.  Each project contained within a 
Foundry resource, which provides model hosting and services.

Foundry resource: Azure resource that provides the platform capabilities: 
 - Models
 - Foundry agent service
 - deployment governance
 - monitoring & observability
 - security boundaries
 - quoats and operational controls

Foundry project: workspace inside a Foundry resource where you build AI apps / agents, evaluations.
Project build and manage: 
 - Agents
 - Evaluations
 - Files / datasets
 - Flows (AI logic)
 - Connections
 - Project-specific settings

One foundry resource for team / dept.  Many Foundry projects inside a Foundry resource.

AI Client application: utilizes a model, data and applicatin logic to process and return results.

Responsibilities of server in client-server Foundry: 
1. receive the prompt
2. run the interference on the model
3. apply system instructions, asfety, context and more
4. return the generated output


## Using Microsoft Foundry endpoints

Foundry resources accessed thru API endpoints via REST.

https://<foundary-project>-resource.cognitiveservices.azure.com/openai/deployments/gpt-40/chat/completions?api-version=2024-05-01-preview


2 types of Foundry endpoints:
 - project-level endpoints
 - model endpoints: prompts to deployed models


TODO: Get started with Microsoft Foundry

https://microsoftlearning.github.io/mslearn-ai-fundamentals/Instructions/Exercises/00-explore-foundry.html#clean-up
https://ai.azure.com


# Generative AI Models
Discover models in Foundry's model catalog
  - hub for models
  - Models sold directly by Azure
  - Models from Partners and Community

Evaluate models by task:
  Chat, coding, summarization, embeddings, multimodal, industry  / domain-specific

Deploy models in Foundry:
  - dev type: standars, global batch, regional providioned throughput

Deployment params: 
  - deployment type
  - model version
  - Tokens per minute

Deployment as:
  - compute resources allocated
  - API endpoint 
  - configuration
  - monitoring / logging



Using a generative AI Model 

use model playground in Foundry Portal
configure parameters: 
 - temperature: controls creativity vs. determinism
 - max output tokens  :  caps response length, affects token consumption and throttling behavior.
 - system instructions: sets tone, behavior, tools and guardrails 

playground provides code that can call your Foundry deployment via Responses API

foundry SDK exposes a project client (Foundry-native ops) and an OpenAI-compatible client for calling 
models via the Responses API.

ex.

# pip install openai>=1.3.0
# pip install azure-ai-projects azure-identity openai

import os
from openai import OpenAI

client = OpenAI(
    base_url=f"{os.environ['AZURE_OPENAI_ENDPOINT']}/openai",
    api_key=os.environ["AZURE_OPENAI_API_KEY"]
)

response = client.responses.create(
    model=os.environ["DEPLOYMENT_NAME"],          # e.g., "gpt-4o-mini"
    input=[{"role": "system", "content": "You're a helpful assistant."},
           {"role": "user", "content": "Summarize the key points from our release notes in 3 bullets."}],
    max_output_tokens=300,
    temperature=0.7
)

print(response.output_text)


models = raw intelligence
agents = packaged, task-oriented workers built on top of that intelligence

## Create an agent

agent = model + instructions + tools

agents can:
 - call external tools 
 - break goals into structured stpes
 - maintain working memory during a conversation
 - process user input, decide actions and generate structured outputs

tools examples:
 - code interpreter
 - using knowledge sources
 - custom functions or APIs

tools allow the model to: 
 - take real actions (read/write files, search, update systems)
 - execute workflows
 - integrate into entrpise systems

knowledge: documents / datasets
  - pdfs, sharepoint content,  azure storage files, 

use agent from client app w/ Foundry Projects SDK
  - connect to project
  - call it from client using Project API


need agent-id of of agent to call via Foundry Project API
Foundry > agents > playground > code view > open .env variables

sample code: 
# Before running the sample, install the packages:
#    pip install --pre azure-ai-projects>=2.0.0b1
#    pip install azure-identity

from azure.identity import DefaultAzureCredential
from azure.ai.projects import AIProjectClient

myEndpoint = "https://<resource>.services.ai.azure.com/api/projects/<resource-name>"

project_client = AIProjectClient(
    endpoint=myEndpoint,
    credential=DefaultAzureCredential(),
)

myAgent = "learning-agent"
# Get an existing agent
agent = project_client.agents.get(agent_name=myAgent)
print(f"Retrieved agent: {agent.name}")

openai_client = project_client.get_openai_client()

# Reference the agent to get a response
response = openai_client.responses.create(
    input=[{"role": "user", "content": "Tell me what you can help with."}],
    extra_body={"agent": {"name": agent.name, "type": "agent_reference"}},
)

print(f"Response output: {response.output_text}")

# Getting started with text analysis in Azure

general purpose AI models for text analsys:
 - key phrase extraction
 - entity linking
 - sentiment analysis and opinion mining
 - summarization

Azure Language Foundry tools
 - language detection
 - PII

Azure Open AI API (sdk)
 - python file
 - config file


Azure Language SDK


OpenAI API: 
import os
from dotenv import load_dotenv
from openai import OpenAI

# Load environment variables from .env file
load_dotenv()
endpoint = os.getenv("AZURE_OPENAI_ENDPOINT")
api_key = os.getenv("API_KEY")
deployment_name = os.getenv("MODEL_DEPLOYMENT_NAME")

# Create the client object
client = OpenAI(
    base_url=endpoint,
    api_key=api_key
)

# Make a request using the client
message = client.responses.create(
    model=deployment_name,
    input="",
)

# Print the results
print(f"Sentiment: {message.output[0]}")


Azure Language SDK:
pip install azure-ai-textanalytics


AZURE_LANGUAGE_ENDPOINT=https://<your-resource>.cognitiveservices.azure.com/
API_KEY=<your-foundry-key>


# Import packages
import os
from dotenv import load_dotenv
from azure.core.credentials import AzureKeyCredential
from azure.ai.textanalytics import TextAnalyticsClient

# Load environment variables from .env file
load_dotenv()
endpoint = os.getenv("AZURE_LANGUAGE_ENDPOINT")
key = os.getenv("API_KEY")

# Create the client
client = TextAnalyticsClient(endpoint=endpoint, credential=AzureKeyCredential(key))

# Make a request using the client for language detection
text = "¡Hola! Me llamo Josefina y vivo en Madrid, España."
result = client.detect_language([text])[0]


## PII Detection
text = "Maria Garcia called from 020 7946 0958 and asked to send documents to 42 Market Road, London, UK, SW1A 1AA."

result = client.recognize_pii_entities([text])[0]

# Print the results
print("Redacted text:", result.redacted_text)
print("\nEntities found:")
for entity in result.entities:
    print(f"  {entity.text} | category={entity.category} | confidence={entity.confidence_score}")


# Use Azure Language with an Agent

Model Context Protocol (MCP): 
open standard that defines houw AI agents connect to external tools and data sources.

agent connects to MCP server and discovers tools offered.

Azure Language MCP Server: 
 - managed services that exposes Azure Language in Foundry Tools
    - bridge between agent an Azure Language features

https://microsoftlearning.github.io/mslearn-ai-fundamentals/Instructions/Exercises/03b-text-analysis.html


# Getting started with speech in Azure


- speech recognition
- speech synthesis


Azure Speech in Microsoft Foundry Tools: 
  - speech to text
  - text to speech 
  - speech tranlations

## Speech recognition 

Azure Speech: Speect to Text


pip install azure-cognitiveservices-speech


import azure.cognitiveservices.speech as speechsdk

# Set up the speech config using resource endpoint
endpoint_url = "ENDPOINT"
speech_key = "FOUNDRY_KEY"

speech_config = speechsdk.SpeechConfig(
    subscription=speech_key,
    endpoint=endpoint_url
)

# Create a recognizer with microphone input
audio_config = speechsdk.audio.AudioConfig(use_default_microphone=True)
speech_recognizer = speechsdk.SpeechRecognizer(
    speech_config=speech_config, 
    audio_config=audio_config
)

# Event handlers
def recognized_handler(evt):
    print(f"Recognized: {evt.result.text}")

def recognizing_handler(evt):
    print(f"Recognizing: {evt.result.text}")

# Connect event handlers
speech_recognizer.recognized.connect(recognized_handler)
speech_recognizer.recognizing.connect(recognizing_handler)

# Start continuous recognition
speech_recognizer.start_continuous_recognition()
print("Say something...")

# Keep the program running
input("Press Enter to stop...")
speech_recognizer.stop_continuous_recognition()


Transacription can be real-time or batch 


## Speech synthesis

Text to Speech (in Foundry)


import os
import azure.cognitiveservices.speech as speechsdk

# This example requires environment variables named "FOUNDRY_KEY" and "ENDPOINT"
speech_config = speechsdk.SpeechConfig(subscription=os.environ.get('FOUNDRY_KEY'), endpoint=os.environ.get('ENDPOINT'))
audio_config = speechsdk.audio.AudioOutputConfig(use_default_speaker=True)

# The neural multilingual voice can speak different languages based on the input text.
speech_config.speech_synthesis_voice_name='en-US-Ava:DragonHDLatestNeural'

speech_synthesizer = speechsdk.SpeechSynthesizer(speech_config=speech_config, audio_config=audio_config)

# Get text from the console and synthesize to the default speaker.
print("Enter some text that you want to speak >")
text = input()

speech_synthesis_result = speech_synthesizer.speak_text_async(text).get()

if speech_synthesis_result.reason == speechsdk.ResultReason.SynthesizingAudioCompleted:
    print("Speech synthesized for text [{}]".format(text))
elif speech_synthesis_result.reason == speechsdk.ResultReason.Canceled:
    cancellation_details = speech_synthesis_result.cancellation_details
    print("Speech synthesis canceled: {}".format(cancellation_details.reason))
    if cancellation_details.reason == speechsdk.CancellationReason.Error:
        if cancellation_details.error_details:
            print("Error details: {}".format(cancellation_details.error_details))
            print("Did you set the speech resource key and endpoint values?")

## Speech capable agent

Azure Speech Voice Live
Speech to Speech

VoiceLive Service
  - listens to conversation and reponds 
  - combines speech-to-text, reasoning, text-to-speech into one package


pip install azure-ai-voicelive
https://microsoftlearning.github.io/mslearn-ai-fundamentals/Instructions/Exercises/04a-speech.html

# Getting started with computer vision in Azure

computer vision: 
 - defect detection
 - medical imaging analysis
 - shelf monitoirng
 - autonomous vehicles


gpt-4.1 / gpt-4.1-mini / gpt-4.1-nano 
 - general purpose multimodal GPT models


gpt-5 series (5.1, 5.2) 
 - advanced multimodal models


pip install openai

import os
from openai import OpenAI

# Environment variables you set locally or in your app service:
FOUNDRY_KEY = "... your key ..."
ENDPOINT = "https://YOUR-RESOURCE-NAME.openai.azure.com/openai/v1/"
MODEL_NAME = "your-model-deployment-name"  # e.g., "gpt-4.1-mini" deployed as "my-vision-deploy"

client = OpenAI(
    api_key=os.getenv("FOUNDRY_KEY"),
    base_url=os.getenv("ENDPOINT"),
)

image_url = ""

response = client.responses.create(
    model=os.getenv("MODEL_NAME"),  # your deployment name 
    input=[
        {
            "role": "user",
            "content": [
                {"type": "input_text", "text": "What is in this image? Provide 3 bullet points."},
                {"type": "input_image", "image_url": image_url}
            ],
        }
    ],
)

print(response.output_text)



# Image Generation models
text to image inferencing



import os
import base64
from openai import OpenAI

# Required environment variables (example names)
FOUNDRY_KEY="..."
ENDPOINT="https://YOUR-RESOURCE-NAME.openai.azure.com/openai/v1/"
MODEL_NAME="your-gpt-image-deployment-name"  # e.g., "gpt-image-1"

client = OpenAI(
    api_key=os.environ["FOUNDRY_KEY"],
    base_url=os.environ["ENDPOINT"],
)

prompt = "A modern flat illustration of a robot holding a potted plant, clean vector style, pastel colors."

response = client.responses.create(
    model=os.environ["MODEL_NAME"],  # your deployment name in Foundry
    input=prompt,
    tools=[{"type": "image_generation"}],
)

image_base64 = next(
    item.result for item in response.output
    if item.type == "image_generation_call"
)

with open("foundry_generated.png", "wb") as f:
    f.write(base64.b64decode(image_base64))

print("Saved: foundry_generated.png")



# Video Generation models

- Sora 1

# create video job

curl -X POST "https://YOUR-RESOURCE-NAME.openai.azure.com/openai/v1/videos" \
  -H "Content-Type: application/json" \
  -H "api-key: $AZURE_OPENAI_API_KEY" \
  -d '{
    "model": "sora-2",
    "prompt": "A cinematic close-up of raindrops sliding down a neon-lit window at night.",
    "size": "1280x720",
    "seconds": "8"
  }'

## poll job status until complete
curl -X GET "https://YOUR-RESOURCE-NAME.openai.azure.com/openai/v1/videos/{video_id}" \
  -H "api-key: $AZURE_OPENAI_API_KEY"


## downloaded completed video
curl -L "https://YOUR-RESOURCE-NAME.openai.azure.com/openai/v1/videos/{video_id}/content?variant=video" \
  -H "api-key: $AZURE_OPENAI_API_KEY" \
  --output output.mp4


https://microsoftlearning.github.io/mslearn-ai-fundamentals/Instructions/Exercises/05a-image-analysis.html


# Get started with AI powered information extraction in Azure


Microsoft Azure Content Understanding in Foundry Tools
  - extract documents, audio, video


- understand schemas
- analyzers
    - applies AI analysis 




python -m pip install azure-ai-contentunderstanding


import os
from azure.ai.contentunderstanding import ContentUnderstandingClient
from azure.core.credentials import AzureKeyCredential

endpoint = os.environ["FOUNDRY_ENDPOINT"]
key = os.environ["FOUNDRY_KEY"]

client = ContentUnderstandingClient(endpoint=endpoint, credential=AzureKeyCredential(key))

# 1) start analysis with analyzer id + inputs
analyzer_id = "prebuilt-invoice"
inputs = [
    {"url": "https://github.com/Azure-Samples/azure-ai-content-understanding-python/raw/refs/heads/main/data/invoice.pdf"}
]

# 2) wait for the Long Running Operation (LRO) to complete
poller = client.begin_analyze(analyzer_id=analyzer_id, inputs=inputs)  # starts LRO
result = poller.result()  # waits for completion (polling handled by SDK)

# 3) read structured fields + markdown
# The result typically includes extracted "fields" and "markdown" per input content item.
for content in result.contents:
    print(content.markdown)
    print(content.fields)


## Extract information from audio and video



import os
from azure.ai.contentunderstanding import ContentUnderstandingClient
from azure.core.credentials import AzureKeyCredential

# Endpoint and key for your Foundry resource
endpoint = os.environ["FOUNDRY_ENDPOINT"]  # e.g., "https://<resource>.services.ai.azure.com/"
key = os.environ["FOUNDRY_KEY"]

client = ContentUnderstandingClient(
    endpoint=endpoint,
    credential=AzureKeyCredential(key)
)

# Choose a prebuilt analyzer for audio
# (The documents module lists examples like prebuilt-audioSearch / prebuilt-videoSearch.)
analyzer_id = "prebuilt-audioSearch"

# Provide an input audio file (URL shown here; you can swap in your own accessible media URL)
inputs = [
    {"url": "https://<your-host>/samples/voicemail.wav"}
]

# Start analysis (asynchronous long-running operation)
poller = client.begin_analyze(analyzer_id=analyzer_id, inputs=inputs)

# Wait for completion (SDK polls under the hood)
result = poller.result()

# Inspect the structured output (JSON-like objects)
for content in result.contents:
    # Some analyzers may return a transcript and/or extracted fields depending on the analyzer and schema
    print("=== MARKDOWN / TRANSCRIPT (if provided) ===")
    print(getattr(content, "markdown", None))

    print("\n=== EXTRACTED FIELDS ===")
    print(getattr(content, "fields", None))


https://microsoftlearning.github.io/mslearn-ai-fundamentals/Instructions/Exercises/06a-content-understanding.html




     import sys
     import json
        
     from azure.ai.contentunderstanding import ContentUnderstandingClient
     from azure.ai.contentunderstanding.models import AnalysisInput, AnalysisResult
     from azure.core.credentials import AzureKeyCredential
     from azure.core.exceptions import AzureError
     from azure.identity import DefaultAzureCredential
        
        
     def main() -> None:
         # Insert the following configurations.
         # 1) AZURE_CONTENT_UNDERSTANDING_ENDPOINT - the endpoint to your Content Understanding resource.
         endpoint = "https://<your-resource>.services.ai.azure.com/"
        
         # 2) CONTENT_UNDERSTANDING_KEY - your Content Understanding API key (optional if using DefaultAzureCredential).
         key = ""
        
         # 3) FILE_URL - you can replace this with your own URL.
         file_url = "https://contentunderstanding.ai.azure.com/assets/prebuilt/layout_checklist.jpg"
        
         # ANALYZER_ID - the ID of the analyzer to use.
         analyzer_id = "prebuilt-layout"
        
         # API_VERSION - the API version to use.
         api_version = "2025-11-01"
        
         # Set up Content Understanding client.
         credential = AzureKeyCredential(key) if key and "" not in key else DefaultAzureCredential()
         client = ContentUnderstandingClient(endpoint=endpoint, credential=credential, api_version=api_version)
        
         # [START analyze]
         print(f"Analyzing with {analyzer_id} analyzer...")
         print(f"  File URL: {file_url}\n")
        
         try:
             poller = client.begin_analyze(
                 analyzer_id=analyzer_id,
                 inputs=[AnalysisInput(url=file_url)],
             )
             result: AnalysisResult = poller.result()
         except AzureError as err:
             print(f"[Azure Error]: {err.message}")
             sys.exit(1)
         except Exception as ex:
             print(f"[Unexpected Error]: {ex}")
             sys.exit(1)
         # [END analyze]
        
         # [START output_result]
         print("=" * 50)
         print("Analysis result:")
         print("=" * 50 + "\n")
        
         max_display_lines = 50
         result_str = json.dumps(result.as_dict(), indent=2)
         ret_lines = result_str.splitlines()
        
         if len(ret_lines) > max_display_lines:
             print("\n".join(ret_lines[:max_display_lines]))
             print(f"\n {len(ret_lines) - max_display_lines} more lines to be displayed...\n")
         else:
             print(result_str)
         # [END output_result]
        
        
     if __name__ == "__main__":
         main()


     # [START analyze]
     print(f"Analyzing with {analyzer_id} analyzer...")
     print(f"  File URL: {file_url}\n")
        
     try:
         poller = client.begin_analyze(
             analyzer_id=analyzer_id,
             inputs=[AnalysisInput(url=file_url)],
         )
         result: AnalysisResult = poller.result()
     except AzureError as err:
         print(f"[Azure Error]: {err.message}")
         sys.exit(1)
     except Exception as ex:
         print(f"[Unexpected Error]: {ex}")
         sys.exit(1)
     # [END analyze]





     # [START output_result]
     print("=" * 50)
     print("Analysis result:")
     print("=" * 50 + "\n")
    
     max_display_lines = 50
     result_str = json.dumps(result.as_dict(), indent=2)
     ret_lines = result_str.splitlines()
    
     if len(ret_lines) > max_display_lines:
         print("\n".join(ret_lines[:max_display_lines]))
         print(f"\n {len(ret_lines) - max_display_lines} more lines to be displayed...\n")
     else:
         print(result_str)
     # [END output_result]















.
